<!DOCTYPE html>
<html>
  <head>
    <title>EcommercePostgresql</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  </head>

  <body>
    <header data-bs-theme="dark">
      <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Veiculos sistema</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
              <li class="nav-item">
                <a class="nav-link <%= params[:controller] == "home" ? "active" : "" %>" aria-current="page" href="/"><%= t("menu.home") %></a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= params[:controller] == "carros" ? "active" : "" %>" href="/carros"><%= t("menu.carros") %></a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= params[:controller] == "fornecedores" ? "active" : "" %>" href="/fornecedores"><%= t("menu.fornecedores") %></a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= params[:controller] == "fornecedor_tipos" ? "active" : "" %>" href="/fornecedor_tipos"><%= t("menu.tipos_de_fornecedores") %></a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= params[:controller] == "enderecos" ? "active" : "" %>" href="/enderecos"><%= t("menu.enderecos") %></a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= params[:controller] == "stimulus" ? "active" : "" %>" href="/stimulus"><%= t("menu.stimulus") %></a>
              </li>

              <li class="nav-item">
                <a class="nav-link" href="/bandeira?i18n=<%= t("menu.lingua_mudar") %>"><%= t("menu.i18n_mudar") %></a>
              </li>

              <li class="nav-item">
                <%= form_with url: destroy_user_session_path, method: :delete do %>
                  <button type="submit" class="nav-link sair-btn" >Sair</button>
                <% end %>
              </li>
            </ul>
            <form class="d-flex" role="search">
              <input class="form-control me-2" type="search" placeholder="Carro desejado ..." aria-label="Search">
              <button class="btn btn-outline-success" type="submit">Buscar</button>
            </form>

            <span style="color: #fff">&nbsp;<%= current_user.email %></span>
          </div>
        </div>
      </nav>
    </header>

    <main class="container" style="margin-top: 80px">
      <% if flash[:success] %>
        <div class="alert alert-success" role="alert">
          <%= flash[:success] %>
        </div>
      <% end %>

      <% if flash[:error] %>
        <div class="alert alert-danger">
            <h2>Confira o erro abaixo</h2>
            <ul>
                <li><%= flash[:error] %></li>
            </ul>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  </body>
</html>
